#include "tree.h"
#include "../colours.h"

tree::tree(point pos) : tile::tile(pos){
    tile::character = 'T';
    tile::foreColour = colours::green;
}
tree::tree(){
    tile::character = 'T';
    tile::foreColour = colours::green;
}
